Fixtures captured 2026-06-11 from the public Kalshi API (`GET /trade-api/v2/markets` and `GET /trade-api/v2/events`, no auth).
Used by automated tests only; tests never touch the network.
This is a static snapshot and must never be presented as live data.
